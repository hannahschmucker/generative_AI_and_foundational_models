# Assignment 1
## Embeddings and Pretraining

*obj: understand what kinds of representations different
sequence pretraining objectives actually learn*

### task(s): train three related sequence models (Unidirectional LSTM LM, 
### Bidirectional LSTM LM, and Seq2Seq encoder-decoder style LM) on the same 
### dataset and run two fixed evaluations (nearest neighbors and cosine similarity)
### to observe how their learned representations differ.


# Assignment 2
## How do LLM's learn?

*obj: understanding causal language modeling, implementing response-only supervised fine-tuning, 
analyzing representation shift and forgetting, comparing decoding strategies*

### task(s): implement a GPT-style Transformer language model
### from configuration (random initialization) and pretrain it on a small real corpus, (Penn Treebank DS)
### then perform supervised fine-tuning (SFT) and analyze what changes in both behavior and
### representations. 
